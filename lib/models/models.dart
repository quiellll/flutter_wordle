enum Language { english, spanish }
enum ValidationResult { win, continue_, invalid, notInWordList }
enum TileState { empty, wrong, wrongPosition, correct }
enum KeyState { unused, wrong, wrongPosition, correct }